name: Swift (iOS)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 16.4
        run: sudo xcode-select -s /Applications/Xcode_16.4.app

      # (Opsiyonel) Şema adını görmek için:
      - name: List schemes
        run: xcodebuild -list -json -package-path .

      # ÖNEMLİ: Şema adı genelde <PackageName>-Package olur (CoreSession-Package).
      - name: Build for iOS Simulator
        run: |
          xcodebuild \
            -scheme CoreSession-Package \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            build
